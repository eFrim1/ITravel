<Window x:Class="TravelAgency.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TravelAgency"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:TravelAgency.view"
        Title="ITravel" Height="900" Width="1000"
        mc:Ignorable="d"
        >
    <Grid>
        <TabControl Margin="10">
            <TabItem Header="Packages">
                <ContentControl Content="{Binding Package}"/>
            </TabItem>
            <TabItem Header="Clients">
                <ContentControl Content="{Binding Client}"/>
            </TabItem>
            <TabItem Header="Bought Packages">
                <ContentControl Content="{Binding BoughtPackage}"/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
